---
import BaseLayout from '../layouts/BaseLayout.astro';
import Badge from '../components/ui/Badge.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';
import Container from '../components/ui/Container.astro';
import Section from '../components/ui/Section.astro';
import '../styles/global.css';
---
<BaseLayout title="Eclipse Lunar" description="Premium SaaS starter built with Astro & TailwindCSS">
  <div class="relative isolate overflow-hidden">
    <div class="pointer-events-none absolute inset-0 bg-spotlight opacity-80"></div>
    <Container class="relative pb-20 pt-10">
      <Section
        as="header"
        tone="canvas"
        padding="none"
        class="grid gap-10 pt-8 lg:grid-cols-[1.2fr_1fr] lg:items-center"
      >
        <div class="space-y-6">
          <Badge intent="primary">New: Launch SaaS pages faster</Badge>
          <h1 class="text-4xl font-semibold leading-tight sm:text-5xl">
            Ship conversion-first experiences for your SaaS in days, not weeks.
          </h1>
          <p class="text-lg text-muted">
            Eclipse Lunar is a crisp Astro theme tailored for SaaS launches. Craft compelling hero sections, articulate
            value props, and captivate investors with refined gradients and precise typography.
          </p>
          <div class="flex flex-wrap items-center gap-4" id="cta">
            <Button as="a" href="#pricing" size="lg">Start with the template</Button>
            <Button as="a" href="#features" intent="secondary" size="lg">View components</Button>
          </div>
          <div class="flex flex-wrap gap-3 text-sm text-muted">
            <Badge intent="outline" size="sm">TypeScript ready</Badge>
            <Badge intent="outline" size="sm">Tailwind tokens</Badge>
            <Badge intent="outline" size="sm">SEO friendly</Badge>
          </div>
        </div>
        <Card padding="lg" class="space-y-6 bg-white/5">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-muted">Real-time insights</p>
              <p class="text-3xl font-semibold text-white">$128,400</p>
            </div>
            <Badge intent="accent" size="sm">+38%</Badge>
          </div>
          <div class="grid grid-cols-3 gap-3 text-center">
            <Card padding="sm" class="bg-white/5 text-left">
              <p class="text-xs text-muted">Active seats</p>
              <p class="text-xl font-semibold">2,140</p>
            </Card>
            <Card padding="sm" class="bg-white/5 text-left">
              <p class="text-xs text-muted">NPS</p>
              <p class="text-xl font-semibold">67</p>
            </Card>
            <Card padding="sm" class="bg-white/5 text-left">
              <p class="text-xs text-muted">Latency</p>
              <p class="text-xl font-semibold">148ms</p>
            </Card>
          </div>
          <div class="grid grid-cols-2 gap-3">
            <Card padding="sm" class="bg-primary/10">
              <p class="text-sm text-primary-light">Uptime</p>
              <p class="text-2xl font-semibold text-white">99.98%</p>
              <p class="text-xs text-muted">Across multi-region edge</p>
            </Card>
            <Card padding="sm" class="bg-white/5">
              <p class="text-sm text-accent">Deploys</p>
              <p class="text-2xl font-semibold">3,200</p>
              <p class="text-xs text-muted">Per month on autopilot</p>
            </Card>
          </div>
        </Card>
      </Section>

      <Section id="features" tone="frosted" padding="lg" class="mt-16 space-y-6">
        <div class="space-y-3">
          <Badge intent="accent" size="sm">Component kit</Badge>
          <div class="flex items-center justify-between gap-4">
            <div>
              <h2 class="text-3xl font-semibold text-white">Composable primitives</h2>
              <p class="text-sm text-muted">
                Use design tokens and UI atoms to compose repeatable sections, from hero to pricing, in minutes.
              </p>
            </div>
            <Badge intent="outline" size="sm">Tokens based</Badge>
          </div>
        </div>
        <div class="grid gap-6 lg:grid-cols-3">
          <Card interactive>
            <div class="flex items-center justify-between">
              <h3 class="text-xl font-semibold">Button</h3>
              <Badge intent="primary" size="sm">Action</Badge>
            </div>
            <p class="mt-2 text-sm text-muted">
              Layered hover states, focus rings, and semantic variants for primary, secondary, or quiet CTAs.
            </p>
            <div class="mt-4 flex flex-wrap gap-3">
              <Button size="sm">Primary</Button>
              <Button intent="secondary" size="sm">Secondary</Button>
              <Button intent="ghost" size="sm">Ghost</Button>
            </div>
          </Card>
          <Card interactive>
            <div class="flex items-center justify-between">
              <h3 class="text-xl font-semibold">Badge</h3>
              <Badge intent="accent" size="sm">Status</Badge>
            </div>
            <p class="mt-2 text-sm text-muted">Signal new features, availability, or usage tiers with tailored colors.</p>
            <div class="mt-4 flex flex-wrap gap-2">
              <Badge intent="primary" size="sm">Live</Badge>
              <Badge intent="accent" size="sm">Beta</Badge>
              <Badge intent="neutral" size="sm">Docs</Badge>
            </div>
          </Card>
          <Card interactive>
            <div class="flex items-center justify-between">
              <h3 class="text-xl font-semibold">Container & Section</h3>
              <Badge intent="outline" size="sm">Layout</Badge>
            </div>
            <p class="mt-2 text-sm text-muted">
              Keep spacing, radii, and glassmorphism consistent across every slice of your marketing site.
            </p>
            <Section tone="solid" padding="sm" class="mt-4 text-sm text-muted">
              <p class="font-semibold text-foreground">Section example</p>
              <p>Drop cards, copy, and CTAs in a padded, token-driven wrapper.</p>
            </Section>
          </Card>
        </div>
      </Section>

      <Section id="metrics" tone="solid" padding="lg" class="mt-16 bg-surface/80">
        <div class="flex flex-wrap items-center justify-between gap-4">
          <div>
            <Badge intent="primary" size="sm">Proof</Badge>
            <h2 class="mt-3 text-3xl font-semibold text-white">Performance metrics</h2>
            <p class="text-sm text-muted">Ready-to-drop stat grids backed by tokens for spacing and borders.</p>
          </div>
          <Badge intent="outline" size="sm">Realtime</Badge>
        </div>
        <div class="mt-6 grid gap-6 lg:grid-cols-4">
          <Card padding="lg">
            <p class="text-sm uppercase tracking-[0.2em] text-muted">Deploy rate</p>
            <p class="text-3xl font-semibold">4m 12s</p>
            <p class="text-sm text-muted">Median build-to-production</p>
          </Card>
          <Card padding="lg">
            <p class="text-sm uppercase tracking-[0.2em] text-muted">Teams</p>
            <p class="text-3xl font-semibold">180+</p>
            <p class="text-sm text-muted">Scaling with the template</p>
          </Card>
          <Card padding="lg">
            <p class="text-sm uppercase tracking-[0.2em] text-muted">Satisfaction</p>
            <p class="text-3xl font-semibold">4.9/5</p>
            <p class="text-sm text-muted">Across product teams</p>
          </Card>
          <Card padding="lg">
            <p class="text-sm uppercase tracking-[0.2em] text-muted">Support</p>
            <p class="text-3xl font-semibold">24/7</p>
            <p class="text-sm text-muted">Fast onboarding help</p>
          </Card>
        </div>
      </Section>

      <Section id="pricing" tone="frosted" padding="lg" class="mt-16">
        <div class="grid gap-8 lg:grid-cols-[1fr_0.9fr] lg:items-center">
          <div class="space-y-4">
            <Badge intent="primary" size="sm">Pricing</Badge>
            <h2 class="text-3xl font-semibold text-white">Simple and transparent</h2>
            <p class="text-muted">
              Start free, then upgrade when you are ready to launch. No hidden fees, cancel anytime.
            </p>
            <ul class="space-y-3 text-sm text-foreground/90">
              <li class="flex items-center gap-3">
                <span class="text-primary">◆</span>
                <span>Unlimited pages with semantic defaults.</span>
              </li>
              <li class="flex items-center gap-3">
                <span class="text-primary">◆</span>
                <span>Customizable Tailwind design tokens.</span>
              </li>
              <li class="flex items-center gap-3">
                <span class="text-primary">◆</span>
                <span>Type-safe integrations and linted workflows.</span>
              </li>
            </ul>
          </div>
          <Card padding="lg" class="bg-white/5 text-slate-100">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-sm text-muted">Launch plan</p>
                <p class="text-4xl font-semibold text-white">$49</p>
                <p class="text-sm text-muted">One-time template license</p>
              </div>
              <Badge intent="primary" size="sm">Popular</Badge>
            </div>
            <div class="mt-5 grid gap-3 text-sm text-foreground/90">
              <p>All Astro + Tailwind setup included</p>
              <p>Access to component updates</p>
              <p>Guides for deployment and analytics</p>
            </div>
            <Button as="a" href="#cta" size="lg" fullWidth class="mt-6">Purchase now</Button>
          </Card>
        </div>
      </Section>
    </Container>
  </div>
</BaseLayout>
